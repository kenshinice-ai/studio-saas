# StudioSaaS design system

**GENERATED by `docs/design/build_spec.py`. Do not hand-edit — regenerate.**
`backend/tests/test_design_lab.py` fails on any difference.

See it rendered: [`docs/design/lab.html`](lab.html) — 3 theme-modes ×
47 components, with every assertion live. Regenerate with
`python3 docs/design/build_lab.py`.

---

## 1. What a colour is here

No colour in this product is chosen. Every one is **solved** for a measured
WCAG target from a small set of declared inputs — a hue, a saturation, a
harmony — by `docs/design/palette_gen.py`. 3 theme-modes × 60
pairs = 180 assertions run on every build, plus the semantic-role
and layering checks.

1 of the themes are offered to studios. One is the platform console,
which is `internal` and never appears in the theme picker.

### The layering rule — first, because contrast cannot express it

Before v8.3.0 all eight dark palettes were arranged upside down and **every
contrast assertion passed**. The generator had mirrored the light lightnesses
around mid-grey, which preserved the distance between surfaces and inverted
what it meant: the alternating band came out brighter than the cards sitting on
it, so the cards read as holes.

> **What must survive a mode change is the ORDER OF PROMINENCE, not the
> arithmetic distance.**

Two assertions carry it:

1. the panel is the most prominent surface in **both** modes — a card is never
   out-shouted by the band it sits on;
2. the band's step away from the page is the same order of magnitude in both
   modes (`LAYER_STEP_TOLERANCE = 1.6`).

A third rule has the same shape and applies to tints: `CEILINGS` asserts an
**upper** bound, because "too loud" is as wrong as "too faint".

### The tokens

| Token | Role | Solved against | Floor |
|---|---|---|---|
| `--bg` | the page | — | — |
| `--bg2` | an alternating band | the page | ordering, not contrast |
| `--panel` / `--surface` | a card — the nearest surface | the page | ordering, not contrast |
| `--surface-hover` | a row or card under the cursor | the panel | 1.06 exactly |
| `--ink` | headings and primary copy | the worst surface | 13:1 light / 11:1 dark |
| `--ink2` | secondary copy | the worst surface | 8.0:1 |
| `--muted` | captions, helper text | the worst surface | 4.6:1 |
| `--line` | a divider | the page | 1.18:1 presence |
| `--line-strong` | an input or control boundary | the worst surface | 3.05:1 (WCAG 1.4.11) |
| `--accent` | the primary action | the worst surface | 4.6:1, or a theme's own target |
| `--on-accent` | a label on any solid role fill | that fill | 4.5:1 |
| `--on-accent-muted` | secondary ink on an accent-filled region | the accent | 4.6:1 |
| `--accent-hover` | hover | rest | 1.06 apart |
| `--accent-pressed` | pressed | hover | 1.06 apart |
| `--accent-soft` | a tinted chip | the panel | 1.22 exactly, capped at 1.70 |
| `--on-accent-soft` | the label on that chip | the chip | 4.5:1 |
| `--accent-border` | the chip's own edge | the chip | 1.45 exactly |
| `--accent-2` | the support colour | the worst surface | 4.6:1 |
| `--accent-2-soft` | a quiet secondary chip | the panel | 1.22 exactly |
| `--on-accent-2-soft` | a quiet secondary chip | the panel | 1.22 exactly |
| `--accent-2-border` | a secondary chip's edge | that chip | 1.45 exactly |
| `--success` | good news, as a fill or as text | page, band, panel and the label on it | 4.6 / 3.0 / 4.5 |
| `--success-soft` | a quiet success chip | the panel | 1.22 exactly |
| `--on-success-soft` | a quiet success chip | the panel | 1.22 exactly |
| `--success-border` | a success chip's edge | that chip | 1.45 exactly |
| `--warning` | something needs attention | page, band, panel and the label on it | 4.6 / 3.0 / 4.5 |
| `--warning-soft` | a quiet warning chip | the panel | 1.22 exactly |
| `--on-warning-soft` | a quiet warning chip | the panel | 1.22 exactly |
| `--warning-border` | a warning chip's edge | that chip | 1.45 exactly |
| `--danger` | bad news or a destructive action | page, band, panel and the label on it | 4.6 / 3.0 / 4.5 |
| `--danger-soft` | a quiet danger chip | the panel | 1.22 exactly |
| `--on-danger-soft` | a quiet danger chip | the panel | 1.22 exactly |
| `--danger-border` | a danger chip's edge | that chip | 1.45 exactly |
| `--info` | a notice that is neither good nor bad | page, band, panel and the label on it | 4.6 / 3.0 / 4.5 |
| `--info-soft` | a quiet info chip | the panel | 1.22 exactly |
| `--on-info-soft` | a quiet info chip | the panel | 1.22 exactly |
| `--info-border` | a info chip's edge | that chip | 1.45 exactly |
| `--focus-ring` | keyboard focus | every surface it can land on | 3.2:1 |
| `--disabled-surface` | a flattened control | the band | — |
| `--disabled-text` | a label that must look unavailable | the disabled surface | 3.0:1 — never 4.5 |
| `--scrim` | the layer under a modal | — | 40–60% opacity |

### Worst measured value of each pair

| Pair | Floor | Worst measured | Where |
|---|---|---|---|
| body / page | 4.5 | **14.18** | studio dark |
| body / panel | 4.5 | **11.01** | studio dark |
| soft / page | 4.5 | **8.82** | studio light |
| muted / page | 4.5 | **5.05** | studio light |
| muted / panel | 4.5 | **4.64** | studio dark |
| muted / alt | 4.5 | **4.60** | platform-console light |
| accent / page | 4.5 | **6.81** | studio light |
| on-accent / accent | 4.5 | **7.65** | studio dark |
| on-accent-muted | 4.5 | **4.61** | studio dark |
| 2nd / page | 4.5 | **4.52** | platform-console light |
| success / page | 4.5 | **4.60** | studio light |
| warning / page | 4.5 | **4.60** | platform-console light |
| danger / page | 4.5 | **4.61** | studio dark |
| success fill / alt | 3.0 | **4.20** | studio light |
| warning fill / alt | 3.0 | **4.18** | platform-console light |
| danger fill / alt | 3.0 | **4.19** | platform-console light |
| success fill / panel | 3.0 | **3.61** | studio dark |
| warning fill / panel | 3.0 | **3.58** | studio dark |
| danger fill / panel | 3.0 | **3.58** | studio dark |
| label / success | 4.5 | **4.64** | studio dark |
| label / warning | 4.5 | **4.61** | studio dark |
| label / danger | 4.5 | **4.61** | studio dark |
| line-strong / page | 3.0 | **3.37** | studio light |
| line-strong / panel | 3.0 | **3.07** | studio dark |
| focus ring / page | 3.0 | **3.52** | studio light |
| focus ring / panel | 3.0 | **3.20** | studio dark |
| focus ring / alt | 3.0 | **3.21** | studio light |
| on-accent / hover | 4.5 | **9.30** | studio dark |
| on-accent / pressed | 4.5 | **11.10** | studio dark |
| disabled text | 2.8 | **3.00** | studio dark |
| info / page | 4.5 | **4.60** | studio light |
| info fill / alt | 3.0 | **4.21** | studio light |
| info fill / panel | 3.0 | **3.58** | studio dark |
| label / info | 4.5 | **4.61** | studio dark |
| accent label / soft | 4.5 | **4.50** | studio dark |
| success label / soft | 4.5 | **4.52** | studio dark |
| warning label / soft | 4.5 | **4.51** | studio dark |
| danger label / soft | 4.5 | **4.52** | platform-console light |
| info label / soft | 4.5 | **4.51** | platform-console light |
| 2nd label / soft | 4.5 | **4.53** | platform-console light |

---

## 2. Where a token is wrong even when its value is right

Every defect found in v8.4.0 was of this kind — not a bad colour, a colour used
somewhere its solving does not apply.

- **A border token is not a text colour.** `--line-strong` is solved to
  3.05:1 as a *boundary*. Used as chip text it measured
  3.67:1.
- **A divider is not a surface.** `--line` used as a background put a muted
  caption on a colour darker than the band every text token is solved against;
  it measured 4.01:1 against a 4.6 floor.
- **An inverted band inverts its whole vocabulary.** A section with
  `background: var(--ink)` must re-declare `--muted`, `--ink2`, `--line` and
  the accent, or any global class dropped inside keeps painting for the page.
  The portal's `.arw` had never cleared 3:1 in *either* mode.
- **A fallback is a hardcoded colour with a longer fuse.** `var(--brand,
  #3b82f6)` kept painting Tailwind blue after `--brand` was renamed away —
  silently, with every stylesheet assertion still green, because the rule lived
  in a `.js` file.
- **`color-scheme` is the only control over native chrome.** Scrollbars, the
  `<select>` popup, checkbox and radio boxes, autofill and the caret read no
  custom property.

---

## 3. Scales

- **Spacing** (Fibonacci): 5px · 8px · 13px · 21px · 34px · 55px · 89px
- **Type** (φ): 13px · 16px · 21px · 34px · 55px
- **Split**: 61.8% / 38.2%
- **Reading measure**: 55ch
- **Motion**: enter 233ms, exit 144ms (exit ≈ 62% of enter, so the interface feels responsive rather than sluggish)
- **Touch**: 44px minimum, 8px between targets

A pseudo-element cannot carry a touch target: Chrome does not hit-test a form
control's `::before` as the control. The 44px box must be the element the
browser dispatches the click to.

---

## 4. Components

47 across 8 groups, merged from the class names the two
consoles and the public portal declare.

- **Surfaces** (6) — Page, Alternating band, Card, Row hover, Disabled surface, Scrim
- **Ink** (4) — Heading, Body, Muted, Disabled
- **Edges** (3) — Divider, Control boundary, Focus ring
- **Actions** (8) — Primary, Primary hover, Primary pressed, Secondary, Outline, Quiet, Danger, Disabled
- **Form** (11) — Text, Placeholder, Textarea, Select, Date, Time, Checkbox, Radio, Colour, Disabled field, Invalid
- **Feedback** (8) — Success, Warning, Danger, Info, Solid success, Solid danger, Progress, Empty state
- **Data** (3) — Table, Stat, Link
- **Content** (4) — Hero, Inverted band, Course card, FAQ

---

## 5. Who decides light or dark

The studio, by default — a studio's brand is the studio's decision, and the
visitor's OS does not get a vote on it.

A studio may hand the choice over by setting the appearance to **follow the
visitor's device**. That requires both palettes to be published, so it is
refused — in the API and disabled in the console — for a theme that ships one
mode. `arcade-lime` is dark only: its accent turns olive on a light page and
loses the reason it exists.

The two consoles are **light only**. They are worked in daylight against a warm
paper that is already easy on the eyes for a long session, and a second mode
would double the surface area of every console change for a use nobody asked
for.

---

## 6. Working on this

1. Change the theme's entry in `THEMES` — the inputs, never a hex.
2. `python3 docs/design/palette_gen.py` — 180 assertions.
3. `python3 docs/design/palette_gen.py --emit-presets` into `presets.py`, and
   `--emit-console-css` into `assets/console-theme.css`.
4. `python3 docs/design/build_lab.py && python3 docs/design/build_spec.py`.
5. Run the suite. A new check must be run against the OLD code first and seen
   to fail; a check that has never failed is a check that proves nothing.
6. A changed static asset needs a new `APP_VERSION`. Versioned assets ship
   `immutable, max-age=31536000`, so redeploying the same version cannot reach
   any browser that already loaded it.

`docs/design/lab.html` is where to look before and after. Its **Tune** mode
moves the generator's inputs and re-solves through `docs/design/solver.js`,
which is checked token-for-token against the Python under node — so what the
sliders show is what the build will produce.
