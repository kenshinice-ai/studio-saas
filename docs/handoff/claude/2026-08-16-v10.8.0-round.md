# 2026-08-16/17 v10.8.0 执行轮

> 状态：门禁全绿，进入发布。验证：完整 pytest（双角色 PostgreSQL）**2747 passed / 7 skipped**；
> `verify_local.sh` **All checks passed**（legacy smoke 73/73、隔离 254/254、迁移/媒体衍生图当前）；
> `release_preflight.sh` clear。发布证据（包 hash、部署、生产健康、浏览器验收）随部署补记于文末。
>
> 门禁路上修的三处「新门撞旧世界」：三个账务测试夹具与隔离种子的开票资料补全地址（E6 门要求
> 名称/地址/ABN——夹具比真实租户简单就是在测不存在的工作室）；本地测试库清掉 2 条指向不存在
> 文件的旧版 showcase 种子媒体行；时间线重试按钮 36px→44px 触点合同。
> 后端子代理在收尾时因用量上限中断，其余工作由主会话完成并验证。

> 状态：**进行中**。Lee 已明确授权：本轮完成后 commit → package → push → deploy 上线。
> 范围：`docs/design/Full_System_Audit_Plan_2026-08-16.md`（rev2）的 Batch A–F 一次做齐：
> A（UI-01..05 界面缺陷）、B（可实施的运维项：OPS-03 nginx 收编、OPS-04 备份 URL 加固、
> OPS-05 文档定性、异地备份/告警脚本就绪化——激活仍需 Lee 提供对象存储/探测账号）、
> C（REL-01 一键发布编排、REL-02 打包双轨清理、REL-04 esbuild 锁定、REL-06 UI 矩阵脚本）、
> D（PROD-01 PDF 验收）、E（E1 学员时间线、E2 付款方月结单、E3 逾期跟进、E4 低课时续费、
> E5 审批重复候选、E6 开票信息门）、F（X1 账务真值补齐；X2 起仍以 Xero developer 账号为硬门，
> 本轮不做 OAuth/transport）。
> 版本：v10.8.0。计划验证：完整 verify_local（双角色 PostgreSQL）+ 真实浏览器验收矩阵 +
> headless 打印 PDF 质量检查 + 生产 deep health。

## 进度（滚动更新）

- 已完成（CMS 车道，本人）：UI-02 守卫分流（401/403/网络三态，SaaS 删除 python3 server.py 文案）；
  E4 低课时一键续费（指挥台数字全部可点 + 预警卡逐人「去充值」）；E1 学员时间线组件；
  E3 发票「记录提醒」+ 工作台应收卡 + 事件历史词条；E2 付款方月结单（视图+打印）；
  E5 审批重复候选弹窗（并入/新建显式选择）；E6 开票门 409 前端文案；
  打印隔离修复：print 只显示 .invoice-print-target 内的客户单据（发票/贷记单/月结单互不串印）；
  cms-i18n 新词条；test_cms_panels 更新+新增（20 过）。bundle 语法编译通过（正式重建待集成后）。
- 新增用户要求：①租户隐私说明扩写为 APP 对齐详版（双语草稿已备）；②用户手册补发票/财务/新功能章节+截图。
- UI-04 已在生产复现：冷加载带 #anchor 完全不滚动（scrollY=0），非仅偏移；待公共面车道修复。
- 并行车道（子代理）：后端 E 系列 API、公共面/管理面 UI-01/03/05、Batch B/C 脚本——进行中。

（完成后本节改写为事实记录：实际改动、验证命令与结果、四层身份、未完成项与风险。）
