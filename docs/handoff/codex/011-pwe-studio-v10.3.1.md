# PWE Studio v10.3.1 — 造世界的脚本归属主

> v10.3.0 让 RLS 在生产上真正生效，同时**打挂了演示重置**：那个脚本以应用
> 角色跑，而它的工作正是「在任何租户上下文之前把世界建出来」—— RLS 下它一行
> 都写不进去。这是对的行为，错的是脚本的身份。
>
> 修法不是逐个打补丁：`db.use_owner_connection()` 一个函数，七个造世界的脚本
> 各调一次（重置、六个种子/导入工具）。六份各写一遍 os.environ 交换，就是六份
> 会各自漂的副本 —— 这一周已经在这上面栽过七次。
>
> **另一个必须记下的更正**：v10.3.0 的说明里写「应用以超级用户连库」是**错的**。
> 我把自己 psql 会话的 `current_user` 当成了应用的。真相是 v7.7.7 就已经把
> 角色分离做完了（compose 里迁移用属主、应用用受限），我提的「方案 1」提的是
> 一个早已存在的东西。核实方式本该是一条命令：
> `docker exec pwestudio-app-1 printenv STUDIOSAAS_DATABASE_URL`。

