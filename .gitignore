# ── macOS / iCloud artifacts ──
__MACOSX/
.DS_Store
*.swp
*~

# ── Python ──
*.pyc
__pycache__/
*.pyo
*.egg-info/
dist/
build/

# ── Virtual environment ──
.venv/
venv/

# ── StudioSaaS local secrets (NEVER commit) ──
backend/.api_secret
backend/.cms_password
backend/.cms_config.json
backend/database.json
backend/backups/
backend/photos/
backend/portfolio/
backend/static/uploads/
backend/media/

# ── Checkpoint archives ──
checkpoints/

# ── Local packaging artefacts ──
*.zip

# ── Local repair backups ──
*.bak
*.bak.*
*.backup

# ── Local test artifacts ──
cms_test_*/
test_*/

# ── OS generated ──
Thumbs.db
ehthumbs.db
