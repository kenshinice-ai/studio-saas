[pytest]
# The two backend/test_*.py files are executable integration/smoke programs,
# not pytest modules. Importing them during collection starts services and
# mutates global Flask state before the isolated fixtures are configured.
# Keep the default pytest gate deterministic; release automation runs both
# standalone programs explicitly after this suite.
testpaths = backend/tests
