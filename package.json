{
  "name": "studiosaas-build-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Pinned build toolchain for StudioSaaS. esbuild produces deterministic browser bundles; Three.js powers the optional product-home spatial scene. Install with `npm ci`.",
  "devDependencies": {
    "esbuild": "0.25.12",
    "three": "0.185.1"
  }
}
