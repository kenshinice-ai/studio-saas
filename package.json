{
  "name": "studiosaas-build-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Pinned build toolchain for StudioSaaS. Not an application package: the only dependency is the esbuild release used by backend/scripts/build_cms.sh, pinned here so the CMS bundle is byte-reproducible across machines and CI (REL-04). Install with `npm ci`.",
  "devDependencies": {
    "esbuild": "0.25.12"
  }
}
